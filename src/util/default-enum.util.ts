export enum HTMLInputTypes {
  // DATE = 'date',
  EMAIL = 'email',
  NUMBER = 'number',
  PASSWORD = 'password',
  TEXT = 'text',
  DATE = 'date',
  // TIME = 'time',
  // FILE = 'file',
  CHECKBOX = 'checkbox',
}

export enum DataType {
  STRING = 'string',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  DATE = 'Date',
}
